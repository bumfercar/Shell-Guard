{
  "name": "Linux Shell Development",
  "build": {
    "dockerfile": "Dockerfile"
  },

  // 컨테이너 실행 후 bash로 시작
  "postCreateCommand": "echo '🚀 Linux Shell Development Environment Ready!'",

  // VSCode 설정
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "timonwong.shellcheck",
        "foxundermoon.shell-format"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash",
        "files.eol": "\n"
      }
    }
  },

  // 특권 모드 (일부 시스템 콜 테스트를 위해 필요할 수 있음)
  "privileged": true,

  // 볼륨 마운트
  "mounts": [
    "source=${localWorkspaceFolder},target=/workspace,type=bind"
  ],

  "workspaceFolder": "/workspace",

  // 컨테이너 실행 유지
  "overrideCommand": false
}
